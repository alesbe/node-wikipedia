import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import SearchTree from "./components/SearchTrees/SearchTrees"

ReactDOM.render(
  <React.StrictMode>
    <SearchTree />
  </React.StrictMode>,
  document.getElementById("root")
);